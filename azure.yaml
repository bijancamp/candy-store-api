# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: candy-store-api
metadata:
  template: fastapi-on-azure-functions@0.0.1-beta
services:
  api:
    project: .
    language: py
    host: function